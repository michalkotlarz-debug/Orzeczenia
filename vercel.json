{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["fra1"],
  "functions": {
    "app.py": {
      "maxDuration": 60,
      "excludeFiles": "{tests/**,deploy/**,.github/**,dane/**,**/__pycache__/**,**/*.pyc}"
    }
  }
}
