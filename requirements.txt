httpx>=0.27
lxml>=5.0
beautifulsoup4>=4.12
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
PyYAML>=6.0
typer>=0.12
# Sterownik PostgreSQL - potrzebny TYLKO wtedy, gdy DATABASE_URL wskazuje
# Postgresa (Neon / Supabase / Railway). Przy SQLite nie jest w ogóle importowany.
psycopg[binary]>=3.1
# Wyciąganie tekstu z PDF-ów aktów prawnych, które nie mają jeszcze HTML-a
# (sources/sejm_eli.py) - sam PDF nigdy nie trafia na dysk.
pdfminer.six>=20231228
# Wykrywanie tabel w tych samych PDF-ach po geometrii (nie samym liniowym
# tekście jak pdfminer) - pdfminer.six jest jego zależnością, więc to nie
# dokłada drugiego, niezależnego silnika PDF.
pdfplumber>=0.11
